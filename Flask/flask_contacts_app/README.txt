#Hay que tener en cuenta que al instalar pyhton se debe serleccionar que se ingresen las variables de entorno(environment)
#Aunque de todas maneras eso se puede configurar después


#Para instalar mysqldb
pip install --user flask-mysqldb

#HAY PROBLEMAS PARA INSTALAR FLASK-MYSQL
Osea el comando pip install flask-mysqldb